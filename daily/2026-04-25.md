# 2026-04-25

- Personal: fixed `seerr` on `optiplex.markul.net` so it can talk to `radarr.markul.net` again; the real issue was a stale Jellyseerr Radarr `baseUrl` (`/radarr.markul.net`) causing `404`s, not container DNS. Details: [[agents/sessions/2026-04-25-optiplex-seerr-radarr-base-url]].
- Personal: confirmed `optiplex`, `dev`, and `infra` cannot resolve new router DNS names under `markul.local` because `systemd-resolved` does not treat `.local` as ordinary unicast DNS even though the router answers those records. Details: [[agents/sessions/2026-04-25-markul-local-systemd-resolved]].
- Personal: reviewed the latest `markul/docker` commit and found concrete issues in the new Optiplex Kafka listener config, the registry cleanup path assumptions, and the `seerr` DNS workaround. Details: [[agents/sessions/2026-04-25-markul-docker-review]].
- Personal: mapped the current media stack across `infra.markul.net` and `optiplex.markul.net`, including storage roots, `uid=1000` permissions, and a suspicious live `qBittorrent` auto-run hook. Details: [[agents/sessions/2026-04-25-media-stack-map]] and [[personal/tech/media-stack|Media Stack]].
- Personal: reviewed `/home/marat/dev/git/markul/Markul.Arm`, documented `markul.net` and `markul.arm` in the vault, and found a vulnerable `AutoMapper` dependency plus a broken solution-level `dotnet test` path. Details: [[agents/sessions/2026-04-25-markul-arm-repo-review]] and [[personal/markul.net/markul.arm/index|markul.arm]].
- Personal: refactored the Markul Kafka outbox queue to use channel-backed batch consumption instead of the old `EventWaitHandle` bridge, and added targeted queue tests. Details: [[agents/sessions/2026-04-25-markul-arm-outbox-queue-refactor]].
