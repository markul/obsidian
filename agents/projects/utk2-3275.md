---
tags:
  - agents/project
  - status/active
---

# UTK2-3275

## Status

- `active`

## Timestamps

- Created: `2026-04-08 10:47`
- Completed: `not completed`

## Goal

- Track the inbox-based Kafka worker for storing client negatives from DWH client result events in `skp-product-change-workflow-service`

## Plan

1. Keep Phase 1 as-is: inbox support, migration, topic registration, and raw JSON consumer wiring are already merged and should not be reworked.
2. Add the missing Phase 2 configuration surface: `CommonOptions.ClientNegativeAllowedCodes` and the `loanmanager-client-service` OData allowlist entry for lookup by `PinEq` plus `AbsId eq 4188`.
3. Extend `ILoanManagerClientProvider` with a dedicated `PinEq` lookup and cover the exact URI in provider tests.
4. Replace the stub `DwhClientResultEventHandler` with the real handler that filters allowed negative codes from `AppNegatives[]`, resolves the client, logs each branch, and persists `ClientNegative`.
5. Clarify and implement the array-handling rule for `AppNegatives[]`: whether to store the first allowed code or one row per allowed code.
6. Add focused unit tests and run build plus unit-test validation before moving the local ticket to `done`.

## Scope

- Local path: `/home/marat/dev/git/alfa/skp-product-change-workflow-service`
- Repository: [origin](ssh://git@git.moscow.alfaintra.net/ufrkpnsb/skp-product-change-workflow-service.git)
- Branch: `feature/UTK2-3275`
- Related notes:
  - [[work/alfa-bank/tickets/active/utk2-3275|UTK2-3275]]
  - [[agents/projects/utk2-3275-plan|UTK2-3275 plan]]
  - [[daily/2026-04-08]]
  - [[daily/2026-04-09]]
  - [[agents/sessions/2026-04-08-utk2-3275-phase1]]
  - [[agents/sessions/2026-04-09-utk2-3275-skaffold]]

## Validation

- `dotnet build`
- `dotnet test test/Skp.ProductChangeWorkflowService.Tests.Unit/Skp.ProductChangeWorkflowService.Tests.Unit.csproj`

## Current State

- Phase 1 is implemented locally: inbox package wiring, background-only consumer registration, DbContext support, stub handler, EF migration, and forward or revert SQL scripts are in place
- The implementation path is intentionally split into two phases: inbox infrastructure first, then the business handler
- Detailed phased plan: [[agents/projects/utk2-3275-plan|UTK2-3275 plan]]
- DWH topic verification changed the Kafka consumer approach: consume `ufr-kpnsb-dwh-client-result` as raw JSON with `AddOpenApiConsumer<DwhClientResultEvent, DwhClientResultEventHandler>()`, not as the default `MessageEnvelope` consumer
- The local DTO now follows the producer shape with `AppNegatives[]`; the older singular `AppNegative` assumption is superseded
- The Jira-linked worker Confluence page defines the target business flow: keep only negative codes `NB302`, `GP302`, `EI302`, `CD148`, `CR101`, resolve the LM client by `PinEq` through `RelationObjectAbses` with `AbsId eq 4188`, and persist into `client_negatives`
- Skaffold support was added on `2026-04-09`: `skaffold.yaml`, Kustomize manifests, local DB init, Kafka/Postgres/WireMock/Kafka UI pod wiring, Docker cache optimization, and standalone integration-test Docker support
- Inbox implementation was updated for the real DWH raw JSON producer format and verified locally with Skaffold
- Jira development metadata now shows merged delivery PRs for this ticket in both `skp-product-change-workflow-service` and `infra-helm-values`
- Older local notes that mention temporary allowlist values `TEST1` and `TEST2` are obsolete and should not be reused
- Repo comparison on `2026-04-11` found `git diff integration-2...HEAD` is empty, so the current branch carries no extra business-handler implementation beyond what is already merged
- Current repo gaps confirmed by code inspection:
  - `DwhClientResultEventHandler` is still a stub that only logs success and returns `Result.Success()`
  - `ILoanManagerClientProvider` has no lookup method by `PinEq`
  - `appsettings.OData.yaml` has no allowlist for `RelationObjectAbses?$filter=Pin eq ... and AbsId eq 4188`
  - `CommonOptions` does not yet expose `ClientNegativeAllowedCodes`
  - unit tests for the new provider method and handler branches do not exist yet
- There is still one business-rule question to settle during implementation: when `AppNegatives[]` contains multiple allowed codes, store the first matching code or create one row per allowed code
- Latest recorded implementation validation:
  - `dotnet build` passed
  - `dotnet test test/Skp.ProductChangeWorkflowService.Tests.Unit/Skp.ProductChangeWorkflowService.Tests.Unit.csproj` passed with `86` tests
- The next repo-facing check should reconcile the merged PR contents with the older Phase 2 reminders before the local ticket is closed

## Key Decisions

- Use `Skp.Kafka.Inbox 12.0.13` with the default serializer
- Use the Confluence allowlist `NB302`, `GP302`, `EI302`, `CD148`, `CR101`; the earlier temporary `TEST1` and `TEST2` note is obsolete
- Consume the DWH Kafka message as raw JSON using built-in inbox `AddOpenApiConsumer`; do not add a custom raw JSON consumer while the package helper covers this case
- Keep the transport notes aligned with the verified producer shape `AppNegatives[]` even though older docs and earlier local notes used a singular `AppNegative`
- For local Skaffold logs, Skaffold `v2.18.2` does not support `skaffold dev --container`; use `skaffold dev -p api --port-forward --tail=false` and tail only the API container with `kubectl logs`
- Do not include Redis-based inbox result publishing unless deployment requirements later require it

## Blockers

- None
