---
tags:
  - agents/project
note-type: agent-project
status: done
created: 2026-04-24T10:53
completed: 2026-04-27T06:59
related-ticket: "[[work/alfa-bank/tickets/utk2-3326|UTK2-3326]]"
---

# UTK2-3326 Agent Work

## Status

- `done`

## Timestamps

- Created: `2026-04-24 10:53`
- Completed: `2026-04-27 06:59`

## Goal

- Support implementation and validation work for `UTK2-3326`

## Plan

1. Keep the ticket note aligned with the current `feature/UTK2-3326` branch state and validation outcome.
2. Keep the active runtime image path on the fixed corporate `.NET 10` Alpine tags in `devops/Dockerfile`.
3. Re-run the local `skaffold` validation only if the image tags or runtime base change again before delivery.

## Scope

- Ticket: [[work/alfa-bank/tickets/utk2-3326|UTK2-3326]]
- Repository: `ufr-kpnsb-limit-service`
- Local path: `/home/marat/dev/git/alfa/ufr-kpnsb-limit-service`
- Branch: `feature/UTK2-3326`
- Related notes:
  - [[daily/2026-04-24]]

## Validation

- `skaffold run -p api`
- `kubectl exec deploy/ufr-kpnsb-limit-service -c ufr-kpnsb-limit-service -- apk info -vv musl libssl3`
- `kubectl exec deploy/ufr-kpnsb-limit-service -c integration-tests -- pytest`
- `dotnet test test/Ufr.Kpnsb.LimitService.Tests.Unit/Ufr.Kpnsb.LimitService.Tests.Unit.csproj -c Release --filter "FullyQualifiedName~Ufr.Kpnsb.LimitService.Tests.Unit.Application.Services.LimitChangeServiceTests"`

## Current State

- Local delivery is complete: `feature/UTK2-3326` was merged into `dev`, and the remediation is no longer just branch-local
- `devops/Dockerfile` now uses `eco-docker-releases.../dotnet/sdk:v10.3.23.16` and `.../dotnet/aspnet:v10.3.23.16`
- The active service image path is the `skaffold` `api` profile with `devops/Dockerfile`; the root `Dockerfile` remains a legacy local-only path and is not the ticket delivery target
- The fixed corporate `.NET 10` Alpine tags contain `musl 1.2.5-r23` and `OpenSSL 3.5.6-r0`, matching the two linked AppSec findings
- The ticket history is now captured in the work-ticket note as a short progression from affected `v10.3.23.12`, through verified fixed candidates `v10.3.23.13` to `.16`, to the current branch state
- Recorded local validation for the updated runtime path is green: `skaffold run -p api` succeeded, the deployed service container reports the fixed packages, and the deployed integration suite passed with `17 passed / 1 skipped / 0 failed`
- AppSec Docker scan evidence now shows the vulnerability as fixed for the delivered image path: [proof](https://appsec/#/repos/63e5eea1da39647f1392a32c/sast/issues/63e5eea1da39647f1392a32c/6704d182a20abb3492953d62?issue_type=docker)
- Jenkins build `ufr-kpnsb-limit-service #1740` for the same commit failed after unit and integration tests passed; the fatal step is the shared `PublishDockerImageStage` trying to `docker tag` a temp registry image that is no longer present locally on the agent
- Jenkins build `ufr-kpnsb-limit-service #1744` on merged `dev` failed on `LimitChangeServiceTests.ProcessLimitGroupAsync_ShouldCreateChangeLimitRequest`; local investigation tied the flakiness to the unit-test setup, where random AutoFixture LM id collisions can overwrite the `SingleOpenLimitStateAsync` mock for the group limit
- The local workspace now includes a test-only stabilization patch for `LimitChangeServiceTests`: make LM identifiers unique before mock setup and remove a random `ChangeLimitAsync` callback branch that did not match the production success path

## Key Decisions

- Treat `devops/Dockerfile` plus the `skaffold` `api` profile as the canonical runtime path for this ticket
- Keep the ticket scoped to the corporate `.NET 10` Alpine image line and do not treat the legacy root `Dockerfile` as the primary delivery path
- Keep the durable implementation context in this project note and keep the work-ticket note minimal
- Treat Jenkins build `#1740` as a pipeline or agent-image-locality failure, not evidence that the `UTK2-3326` image-tag change regressed build or test behavior
- Treat Jenkins build `#1744` as a unit-test flake caused by test setup nondeterminism, not by the `devops/Dockerfile` image-tag change that was merged from `UTK2-3326`

## Blockers

- None currently
