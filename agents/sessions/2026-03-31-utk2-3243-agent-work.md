# 2026-03-31 utk2-3243-agent-work

## Goal

- Start the agent-side workspace and durable note structure for `UTK2-3243`
- Capture the CPM migration work performed in `ufr-kpnsb-product-request-workflow-service`

## Scope

- Agent project: [[agents/projects/utk2-3243|UTK2-3243 Agent Work]]
- Repo: `/home/marat/dev/git/alfa/ufr-kpnsb-product-request-workflow-service`
- Related notes:
  - [[work/alfa-bank/tickets/on-hold/utk2-3243|UTK2-3243]]
  - [[daily/2026-03-31]]

## Actions

- Moved `UTK2-3243` from `new` to `active` under `work/alfa-bank/tickets/`
- Updated the ticket note with Jira, Confluence, remote Bitbucket, and local repository references
- Created the stable agent project under [[agents/projects/utk2-3243|UTK2-3243 Agent Work]]
- Updated the vault workflow so meaningful agent work uses session notes under `agents/sessions/`
- Verified that `RestoreTools` in the API project was unused because no local `dotnet-tools.json` exists in the repo
- Removed the `RestoreTools` target from `Ufr.Kpnsb.ProductRequestWorkflowService.Api.csproj`
- Added `Directory.Packages.props` and migrated package versions from project files and `Directory.Build.props`
- Used local repo `/home/marat/dev/git/alfa/ufr-kpnsb-limit-service` as the CPM reference
- Enabled `CentralPackageTransitivePinningEnabled` to match the reference repo
- Fixed CPM restore issues by pinning `System.ServiceModel.*` to `4.10.3`, suppressing `NU1507`, and lifting central versions required by transitive pinning
- Validated the repository with `dotnet build Ufr.Kpnsb.ProductRequestWorkflowService.sln -v minimal`
- Investigated the current branch after `feature/UTK2-3243` had already been merged into `feature/UTK2-3243-dev`
- Confirmed the build break came from package/API skew rather than code conflicts
- Restored `Skp.Kafka`, `Skp.Kafka.Inbox`, and `Skp.Kafka.Outbox` to `4.0.4` for `OutboxSerializerDecorator`
- Restored `Ufr.Kpnsb.ScoringService.Client` to `0.630.1-rc.1` for `ScoringService`
- Rebuilt the current branch successfully after those version corrections

## Decisions

- `UTK2-3243` should have a dedicated stable agent project
- Session continuity for this work should live in `agents/sessions/` and link back to the agent project
- Durable ticket facts stay in the work-ticket note, while agent execution context can live in the agent project and session notes
- Follow the local `ufr-kpnsb-limit-service` CPM pattern rather than inventing a one-off layout
- Keep the repo-level `NU1507` suppression because CPM otherwise fails in the current two-source NuGet environment
- Keep the non-failing `dotnet tool restore` manifest lookup message as a known build artifact for now
- The merge regression should be fixed by keeping the newer package line where the merged branch code already depends on newer APIs

## Follow Up

- Continue the remaining `.NET 10` migration items under `UTK2-3243`
- Decide later whether the residual `dotnet tool restore` message should be removed elsewhere in the build/tooling chain
