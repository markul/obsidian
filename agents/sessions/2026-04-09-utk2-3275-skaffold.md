# 2026-04-09 UTK2-3275 Skaffold

## Scope

- Project: [[agents/projects/utk2-3275|UTK2-3275]]
- Ticket: [[work/alfa-bank/tickets/utk2-3275|UTK2-3275]]
- Local repo: `/home/marat/dev/git/alfa/skp-product-change-workflow-service`
- Branch: `feature/UTK2-3275`
- Goal: add Skaffold support, update inbox consumption for the real DWH message format, and verify local execution with Skaffold

## Actions

- Added local Skaffold support around the service with Kustomize overlays and a single local Deployment containing API, integration-test init, WireMock, Postgres, Kafka UI, Zookeeper, and Kafka.
- Added Kafka topic creation for `skp-product-change-workflow-event` and `ufr-kpnsb-dwh-client-result`; API startup waits for the topic-ready marker before running.
- Added `devops/base/init-db.sh` to run manual SQL scripts for the local Postgres container, skip only `0001-CreateDatabase.sql`, and set the local service role password after scripts complete.
- Optimized `devops/Dockerfile` for Docker layer caching by restoring before copying full source and skipping SonarQube tooling when `SONAR_LOGIN` is absent.
- Added standalone integration-test Docker support on `.NET 8` without project references so the test image can build independently for Skaffold.
- Verified DWH producer shape in `ufr-kpnsb-dwh-integration-service`: the topic is raw JSON on `ufr-kpnsb-dwh-client-result`, so the consumer was switched to inbox `AddOpenApiConsumer`.
- Added a local sample DWH event note under `devops/kafka/`.

## Commands

- `skaffold render -p api --offline`
- `skaffold dev -p api --port-forward=false`
- `docker build -f test/Skp.ProductChangeWorkflowService.Tests.Integration/Dockerfile .`
- `dotnet build --no-restore`
- `skaffold dev --help`
- `skaffold options`
- `skaffold dev --container "skp-product-change-workflow-service"`
- `git diff --cached --name-status`
- `git diff --cached --stat`

## Decisions

- Keep Kafka UI in the local pod for manual message publishing, but quiet Skaffold output with `--tail=false` and tail only API logs via `kubectl logs` when needed.
- Do not rely on a Skaffold `--container` log filter: installed Skaffold `v2.18.2` rejects the flag.
- Use inbox package `AddOpenApiConsumer` for raw JSON rather than adding a service-local custom raw JSON Kafka consumer.

## Validation

- `dotnet build --no-restore` passed after the raw JSON consumer switch.
- `skaffold render -p api --offline` passed.
- Local Skaffold run verified API startup against local Postgres and Kafka topic setup.

## Follow-up

- Phase 2 still needs the real client-negative handler, LM lookup, OData allowlist, and unit tests.
