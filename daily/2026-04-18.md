# 2026-04-18

- Added the first Kibana provider slice to [[agents/projects/utk-mcp|UTK MCP]] with `get_kibana_status` and `search_kibana_logs`; full unit suite is green.
- Extended [[agents/projects/utk-mcp|UTK MCP]] Kibana search with exact `service` and `severity` filters, then validated the last 10 prelive `Error` logs for `ufr-kpnsb-limit-service`.
- Fixed the Docker-backed [[agents/projects/utk-mcp|UTK MCP]] Kibana auth regression caused by doubled backslash escaping in the local `.env`, then revalidated the original `dev` query for `ufr-kpnsb-ews-check-service` through the Codex-facing wrapper.
- Tightened [[agents/projects/utk-mcp|UTK MCP]] Kibana hit normalization so the output `Service` field now comes only from `app_id` or `app`, not `service.name`; Kibana unit tests still pass.
