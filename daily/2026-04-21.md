# 2026-04-21

- Personal: verified and installed `node_exporter` on [[agents/projects/my-infra|My Infra]] host `proxmox` at `192.168.222.3`; the live hypervisor is Proxmox VE `8.4.5` on Debian `12`, `node_exporter 1.11.1` now runs as a systemd service on port `9100`, and the local metrics endpoint responds. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: re-checked live Proxmox storage on `192.168.222.3` and updated [[personal/tech/Infrastructure|Infrastructure]] with the actual backend layout: `local`, `local-lvm`, `hdd500`, `hdd1000`, and `usb-drive2000`. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: fixed local access to `proxmox.markul.net` after moving it to router-local DNS; this workstation now resolves it to `192.168.222.3` and SSH by hostname works again after refreshing the saved host key. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: updated Prometheus on `dev.markul.net` to scrape Proxmox `node_exporter` by hostname `proxmox.markul.net:9100`; after flushing DNS caches on `dev`, the name resolved to `192.168.222.3`, config validation passed, and `monitoring_prometheus` was restarted. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: analyzed `socat-proxmox` on `dev.markul.net`; it is a private Docker-side TCP forward used by `nginx-proxy` to front `https://192.168.222.3:8006`, and it now looks mostly obsolete for LAN use because `proxmox.markul.net` resolves directly to the Proxmox host. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: removed the obsolete `socat-proxmox` path from `dev.markul.net`; the compose file no longer defines it, the container is gone, and `nginx-proxy` now falls through to its default `503` for `Host: proxmox.markul.net` instead of proxying to Proxmox. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: installed `node_exporter 1.11.1` on both VPS nodes `helsinki.markul.net` and `frankfurt.markul.net` as native systemd services on port `9100`; `helsinki` uses the `arm64` build and `frankfurt` uses the `amd64` build. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: confirmed Prometheus on `dev.markul.net` can now scrape both VPS exporters by hostname; `frankfurt` worked immediately, `helsinki` needed `ufw allow 9100/tcp`, and `monitoring_prometheus` was restarted after validation. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: checked recent `nginx-proxy` logs on the public ingress hosts; `dev` did not show clear vulnerability-probe traffic in the sampled window, while `helsinki` showed typical internet probing for `.env`, `.git`, `xmlrpc.php`, WordPress, HNAP, Solr, Boaform, and CGI auth endpoints. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: planned the next observability step for [[agents/projects/my-infra|My Infra]]: ship `nginx-proxy` probe logs from `helsinki.markul.net` into Elasticsearch on `dev.markul.net` with GeoIP enrichment so Grafana can visualize source countries and top probe paths. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: implemented the first `helsinki.markul.net` probe-logging stack for [[agents/projects/my-infra|My Infra]] with local `Loki + Alloy`; it now tails `nginx-proxy` Docker `json-file` logs from the host, classifies suspicious paths such as `/.env` and `/boaform/*`, enriches public client IPs with GeoIP, and stores probe events in Loki under `job="nginx-proxy-probes"`. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Personal: exposed `helsinki.markul.net` Loki on `3100/tcp` for Grafana access from `dev.markul.net` only; `ufw` now restricts that port to source `46.191.235.87`, and a live check from `dev` returns `200` on the Loki labels API. Details: [[agents/sessions/2026-04-21-my-infra-proxmox-node-exporter-readiness]].
- Alfa Bank: finalized and verified the local branch state for [[work/alfa-bank/tickets/utk2-3310|UTK2-3310]] in `skp-product-change-workflow-service`; the repo now keeps default skaffold for dependencies only, uses the `api` profile for dependencies plus API plus CronJobs, and required one Dockerfile restore fix before `skaffold run -p api` built and applied the expected resources. Details: [[agents/sessions/2026-04-21-utk2-3310-skaffold]].
- Alfa Bank: corrected the `UTK2-3310` config split so `appsettings.Local.yaml` now routes downstream service calls through local WireMock while `appsettings.Dev.yaml` preserves the remote dev service, DB, and Kafka endpoints. Details: [[agents/sessions/2026-04-21-utk2-3310-skaffold]].
- Alfa Bank: extracted the Reqnroll integration scenario from `feature/UTK2-3238_temp` into `UTK2-3310`, adapted it to the current single-pod local strategy, and restored in-pod Kubernetes proxy or RBAC support so the test container can read CronJobs and create Jobs locally. Details: [[agents/sessions/2026-04-21-utk2-3310-skaffold]].
