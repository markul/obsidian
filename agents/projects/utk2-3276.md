---
tags:
  - agents/project
note-type: agent-project
status: active
created: 2026-04-17T09:00
completed: 
related-ticket: "[[work/alfa-bank/tickets/utk2-3276|UTK2-3276]]"
---

# UTK2-3276

## Status

- `active`

## Timestamps

- Created: `2026-04-17 09:00`
- Completed: `not completed`

## Goal

- Add the overdue-check workflow algorithm in `skp-product-change-workflow-service`

## Plan

1. Add repository support for checking whether the client has matching negatives during the last year.
2. Add a workflow step and process-context flag for the overdue check.
3. Add focused unit tests for the step and repository behavior.

## Scope

- Local path: `/home/marat/dev/git/alfa/skp-product-change-workflow-service`
- Repository: [origin](ssh://git@git.moscow.alfaintra.net/ufrkpnsb/skp-product-change-workflow-service.git)
- Branch: `feature/UTK2-3276`
- Related notes:
  - [[work/alfa-bank/tickets/utk2-3276|UTK2-3276]]
  - [[work/alfa-bank/tickets/utk2-3275|UTK2-3275]]

## Validation

- `dotnet test test/Skp.ProductChangeWorkflowService.Tests.Unit/Skp.ProductChangeWorkflowService.Tests.Unit.csproj --filter CheckOverdueProcessStepTests`
- Result: `2` passed
- `dotnet test test/Skp.ProductChangeWorkflowService.Tests.Unit/Skp.ProductChangeWorkflowService.Tests.Unit.csproj --filter ClientNegativeRepositoryTests`
- Result: `2` passed
- `dotnet test test/Skp.ProductChangeWorkflowService.Tests.Unit/Skp.ProductChangeWorkflowService.Tests.Unit.csproj`
- Result: `167` passed
- `dotnet build`
- Result: build passed

## Current State

- Jira has no linked branch or PR yet; local work starts from `feature/UTK2-3276`.
- The algorithm Confluence page defines a single input `ClientId` and output `isOverdueCheckSucess`.
- The target rule is: search `client_negatives` for the client, the allowed negative codes, and `negative_date` within the last year; if nothing is found, the check passes.
- The repository already contains `client_negatives` ingestion from [[work/alfa-bank/tickets/utk2-3275|UTK2-3275]], but there is no workflow step yet that consumes those rows.
- `AutoChecksProcess` currently only runs `CheckClientZoneByRatingProcessStep`.
- Implemented locally:
  - `IClientNegativeRepository.HasNegativeCodesSinceAsync(...)`
  - `CheckOverdueProcessStep`
  - `ProductChangeContext.IsOverdueCheckSucess`
  - step and repository unit tests
- The overdue step is intentionally not wired into `AutoChecksProcess`.
- The repository keeps client-id and negative-code filtering in EF and applies the final date cutoff after materialization; this shape was chosen so the behavior stays testable on the shared SQLite unit-test fixture.
- The repository now keeps client-id, negative-code, and date filtering in the EF query.
- The SQLite unit-test context now maps `ClientNegative.NegativeDate` through a test-only UTC `DateTime` conversion so the server-side date comparison remains translatable in repository tests.

## Key Decisions

- Reuse `CommonOptions.ClientNegativeAllowedCodes` as the source of truth for the overdue-negative code list instead of duplicating constants.
- Put the database lookup behind `IClientNegativeRepository` to match the existing EF repository style.
- Keep the new workflow output name aligned with the existing Confluence spelling: `IsOverdueCheckSucess`.
- Keep the repository filter server-side; adjust the SQLite test context rather than falling back to client-side post-filtering.

## Blockers

- None currently
