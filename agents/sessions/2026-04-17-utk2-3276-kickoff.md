---
tags:
  - agents/session
note-type: agent-session
session-date: 2026-04-17
related-project: "[[agents/projects/utk2-3276|UTK2-3276]]"
related-ticket: "[[work/alfa-bank/tickets/utk2-3276|UTK2-3276]]"
---

# 2026-04-17 UTK2-3276 kickoff

## Goal

- Start local implementation of the overdue-check algorithm for `UTK2-3276`

## Scope

- Agent project: [[agents/projects/utk2-3276|UTK2-3276]]
- Repo: `/home/marat/dev/git/alfa/skp-product-change-workflow-service`
- Related notes:
  - [[work/alfa-bank/tickets/utk2-3276|UTK2-3276]]
  - [[work/alfa-bank/tickets/utk2-3275|UTK2-3275]]
  - [[daily/2026-04-17]]

## Actions

- Verified Alfa VPN connectivity with `snxctl status`
- Pulled Jira metadata for `UTK2-3276` and the linked Confluence pages via `utk-mcp`
- Confirmed there was no existing local ticket note, agent project note, branch, or repo implementation for `UTK2-3276`
- Created local branch `feature/UTK2-3276`
- Added the vault ticket note, agent project note, and Alfa index entry for `UTK2-3276`
- Read the existing `UTK2-3275` notes and current repo code to reuse the `client_negatives` ingestion work instead of introducing a parallel storage path
- Added `IClientNegativeRepository.HasNegativeCodesSinceAsync(...)`
- Implemented `CheckOverdueProcessStep`
- Added `ProductChangeContext.IsOverdueCheckSucess`
- Added focused unit tests for the new workflow step and repository
- Updated the SQLite test cleanup helper to clear `ClientNegatives` between repository tests
- Ran focused tests, fixed compile issues, and adjusted the repository shape so it remains testable on the SQLite unit-test fixture
- Re-ran the full unit suite and solution build
- Removed the `AutoChecksProcess` wiring so the overdue step remains implemented locally but inactive in workflow execution
- Reworked `ClientNegativeRepository.HasNegativeCodesSinceAsync(...)` back to full server-side filtering
- Added a test-only UTC `DateTime` conversion for `ClientNegative.NegativeDate` in the SQLite unit-test DbContext so the repository query stays translatable

## Decisions

- Reuse `CommonOptions.ClientNegativeAllowedCodes` for the overdue check so the ingestion and validation sides share one code list
- Keep the repository API generic enough to accept a code list and cutoff date, but let the step own the business rule for “last year” and the configured allowed codes
- Apply the final `negative_date > fromDate` comparison after materializing the filtered rows in the repository; SQLite in the shared unit-test fixture would not translate the `DateTimeOffset` comparison cleanly
- Keep the overdue step disconnected from `AutoChecksProcess` for now
- Prefer fixing the SQLite test context over moving repository filtering out of SQL

## Follow Up

- Decide whether `UTK2-3276` needs any workflow wiring at all, and if yes, where it should be connected later
- If remote delivery starts next, prepare the commit and PR from `feature/UTK2-3276`
