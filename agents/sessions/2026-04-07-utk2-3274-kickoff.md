---
tags:
  - agents/session
service: "[[work/alfa-bank/services/skp-product-change-workflow-service|skp-product-change-workflow-service]]"
---

# 2026-04-07 utk2-3274 kickoff

## Scope

- Repo: `/home/marat/dev/git/alfa/skp-product-change-workflow-service`
- Related notes:
  - [[agents/projects/utk2-3274|UTK2-3274]]
  - [[work/alfa-bank/tickets/utk2-3274|UTK2-3274]]

## Goal

- Capture durable context before implementing `UTK2-3274`

## Actions

- Verified Alfa VPN connectivity with `snxctl status`
- Fetched Jira `UTK2-3274` and linked Confluence pages through `utk-mcp`
- Mapped the ticket to the local repo and inspected the real EF Core database layer
- Confirmed that the repo already contains:
  - `ProductChangeDbContext`
  - entity configurations under `Infrastructure/Database/Configuration`
  - checked-in EF migrations
  - checked-in manual forward and revert SQL scripts

## Findings

- Jira summary: `DEV :: Создать таблицу для хранения негативов по клиенту`
- Linked Confluence DB page defines `client_negatives` with:
  - `id`
  - `negative_code`
  - `client_id`
  - `negative_date`
- No branch or PR is linked to the Jira issue yet
- Current local branch is `feature/UTK2-3274`
- Remote branch `feature/UTK2-3274` does not exist yet
- Current local HEAD is `e797886c67bc532934cc5685cdd22ccca7f64380`

## Expected Implementation Path

- Add a new domain entity under `src/Skp.ProductChangeWorkflowService.Domain/Entities`
- Add a new `DbSet` to `src/Skp.ProductChangeWorkflowService.Infrastructure/Database/ProductChangeDbContext.cs`
- Add a new EF config under `src/Skp.ProductChangeWorkflowService.Infrastructure/Database/Configuration`
- Generate a migration from `ProductChangeDbContext`
- Generate matching manual SQL and revert SQL scripts under `Infrastructure/Database/ManualScripts`

## Open Questions

- Whether `negative_code` should be `text` or bounded `varchar`
- Whether `negative_date` should be stored as `timestamp with time zone`
- Whether the table needs uniqueness or additional indexes

## Validation

- `dotnet build Skp.ProductChangeWorkflowService.sln` passed during repo analysis
- Unit tests passed
- Integration test project currently has zero discovered tests
