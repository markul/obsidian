# UTK2-3264 .NET 10 follow-up

## Scope

- Ticket: [[work/alfa-bank/tickets/active/utk2-3264|UTK2-3264]]
- Agent project: [[agents/projects/utk2-3264|UTK2-3264 agent project]]
- Repository: `/home/marat/dev/git/alfa/ufr-kpnsb-limit-service`

## Goal

- Reconcile the WireMock migration branch with the incoming `.NET 10` and central package management changes without breaking integration coverage or the CI path

## Actions

- Monitored the pushed branch in Jenkins and confirmed `ufr-kpnsb-limit-service #1645` completed successfully for commit `27712288fa455d4ef89375f5166891a3972d831e`
- Verified from the Jenkins console that the branch integration tests finished with `11 passed, 1 skipped, 0 failed`
- Analyzed the incoming `integration-1` changes, which combined the `.NET 10` upgrade with AutoMapper removal and replacement by static hand-written mapping methods
- Applied the relevant central package management changes to the branch while keeping the integration-test project on `net8.0`
- Traced the failing integration tests to the new hand-written mappers: they relied on nullable annotations, but OData providers still returned `null` in fields modeled as non-nullable
- Restored AutoMapper-based mapping because the manual mapping approach was not robust against the actual OData payload shapes
- Restored plain local `dotnet build` by opting the integration-test project out of central package management and supplying explicit versions for the inherited analyzer packages
- Split the WireMock DTOs into separate files so the integration-test project passes `SA1402` under the shared analyzer configuration
- Fixed the `002_AddAsync_LimitGroups` integration failure by widening the WireMock `{datetime}` matcher to accept offset-based timestamps as well as `Z`
- Removed the unnecessary null filtering from the `LoanManagerClientProvider` address mapping path after stabilizing the mapper behavior
- Updated `devops/Dockerfile` to copy `Directory.Packages.props` before `dotnet restore`
- Updated `devops/NuGet.Config` to clear inherited sources and use `https://binary.alfabank.ru/artifactory/api/nuget/v3/nuget_public`

## Decisions

- Keep the integration-test project on `net8.0` for now instead of forcing it onto the solution-wide `.NET 10` target
- Keep AutoMapper restored; do not keep the `integration-1` manual static mapper pattern for OData-backed models unless the provider contracts are tightened first
- Keep the Docker restore fix limited to package metadata visibility and source configuration; do not treat the current Artifactory timeouts as a project-file issue
- Preserve the version-agnostic Allure path handling in `devops/RunTests.groovy`

## Validation

- `dotnet build`
- `dotnet test test/Ufr.Kpnsb.LimitService.Tests.Integration/Ufr.Kpnsb.LimitService.Tests.Integration.csproj --no-restore --filter "Name~002_001"`
- Jenkins `ufr-kpnsb-limit-service #1645`

## Blockers

- `docker build -f devops/Dockerfile ...` still stalls in `dotnet restore` because Artifactory package requests time out from inside Docker, even after switching the image restore path to `Directory.Packages.props` plus the v3 NuGet feed

## Follow Up

- Re-run the Docker build when the internal feed is responsive, or diagnose why the Docker network path to Artifactory is materially worse than the host path
