# 2026-04-26

- Personal: added a new HTTPS Docker registry on `helsinki.markul.net` at `https://hub-arm.markul.net/v2/`, backed by a dedicated `hub-arm` compose project and `nginx-proxy` TLS termination. Details: [[agents/projects/my-infra|My Infra]].
- Personal: updated `Markul.Arm` Drone publishing so `amd64` images keep pushing to `hub.markul.net` and `arm64` images now push to `hub-arm.markul.net`, with both versioned build tags and `:dev` tags. Details: [[agents/projects/my-infra|My Infra]].
